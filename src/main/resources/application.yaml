spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 123@Vinicius
    username: postgres
    url: jdbc:postgresql://localhost:5432/ticketoffice
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081